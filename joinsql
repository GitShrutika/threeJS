CREATE TABLE stude(
 id INT PRIMARY KEY,
 name VARCHAR(50)
 );
 
 INSERT INTO stude
 (id,name)
 VALUES
 (101,'shri'),
 (102,'shris'),
 (103,'shrish');
 
 CREATE TABLE course(
 id INT PRIMARY KEY ,
 course VARCHAR(50)
 );
 INSERT INTO course
 (id,course)
 VALUES
 (101,'science'),
 (102,'bio'),
 (103,'math');
 
 SELECT * FROM stude
 INNER JOIN course
 ON stude.id = course.id;
 
 SELECT *
 FROM course as s
 RIGHT JOIN stude
 ON s.id=stude.id;
 SELECT * FROM stude as a
 LEFT JOIN course as b
 ON a.id=b.id
 UNION 
 SELECT * FROM stude as a
 RIGHT JOIN course as b
 ON a.id=b.id
 
 
